name: 'build'
on:
  pull_request:
  push:
    branches:
      - main
      - dev
      - 'releases/*'

jobs:
  test:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
      using: 'node12'
      - name: install
        run: |
          npm install
      - name: run all
        run: |
          npm run all
